# 复制为 .env 并填入真实值（.env 已被 gitignore，不会提交）
# cookie 获取: 浏览器 DevTools -> Application -> Cookies -> https://opencode.ai
#            复制 Cookie 头的完整值，多个 cookie 用 ; 分隔
OPENCODE_COOKIE=
# workspace id（默认值已在 CLI 中，可留空）
OPENCODE_WORKSPACE=
